/* eslint-disable arrow-body-style */
export default (settings) => {
  // todo implement when needed
  // err: `Please use ${USERNAME_MIN_LENGTH} characters at minimum in username`
  return {
    ok: true,
    err: null,
  };
};
